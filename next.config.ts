import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Your standard Next.js config options can go here in the future
  // The blocking COEP/COOP headers have been completely removed
};

export default nextConfig;